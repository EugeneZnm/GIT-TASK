export class User {
    // tslint:disable-next-line:max-line-length
    constructor(public user: string, public name: string, public company: string, public bio: string, public email: string, public public_repos: number, ) {}
}

