export class User {
    // tslint:disable-next-line:max-line-length
    constructor(public user: string, public name: string, public avatar_url: string, public followers: number, public following: number, public public_repos: number, ) {}
}

